<div class="authenticate-content">
    <div class="authenticate-form">
        <div class="authenticate-title">Update password</div>
        <hr>
        {{#if hasErrors}}
            <div class="alert alert-danger">
                {{# each messages}}
                    <p>{{ this }}</p>
                {{/each}}
            </div>
        {{/if}}
        <form action="/user/update-password" method="post">
            <div class="form-group">
                <input type="password" id="password" name="password" class="form-control" placeholder="password">
            </div>
            <div class="form-group">
                <input type="password" id="confirm_password" name="confirm_password" class="form-control" placeholder="confirm password">
                <input type="hidden" name="_csrf" value="{{ csrfToken }}">
                <input type="hidden" name="reset_link" value="{{ resetLink }}">
                <button class="auth-form-btn">CHANGE</button>
            </div>
        </form>
    </div>
</div>
